<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PawNav" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.PawNav" parent="Base.Theme.PawNav" />

    <!--SplashScreen-->
    <style name="Theme.PawNav.Splash" parent= "Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo</item>
        <item name="postSplashScreenTheme">@style/Theme.PawNav</item>
    </style>

    <style name="Theme.YourApp" parent="Theme.MaterialComponents.Light">
        <item name="colorPrimary">@color/deepSlateBlue</item>
        <item name="colorPrimaryVariant">@color/deepSlateBlue</item>
        <item name="colorAccent">@color/deepSlateBlue</item>
    </style>

</resources>